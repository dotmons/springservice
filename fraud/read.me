This is a fraud microservice application running on port 8081 with
a postgres database running on port 5430

To run the database here, lunch a client side database for postgres, and connect
using the datasource in application.yml

password: password
url: jdbc:postgresql://localhost:5430/postgres
username: dotmonsscode

Then run this command:
create database fraud;